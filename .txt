
---- auto delete my accepted bookings
----- paymnet k dekhn hai accpet hone k bad ya phir acpetd k phle -- or agr payment ho jay to accpeted or booking delete
-- bit m bookignCode bhi bhejna hai ok  
phle phase -- bit accepted ,  and booking m driverId and status uska assigned

 <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" strooke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
            </svg>
            my booking

            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>

   <!-- Address Details -->
        <div class="space-y-3 flex justify-between">
          <div class="flex gap-3">
            <div class="flex-shrink-0 w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
              <svg class="w-4 h-4 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"></path>
              </svg>
            </div>
            <div class="flex-1">
              <p class="text-xs text-slate-500 font-semibold mb-1">Pickup Address</p>
              <p class="text-sm text-slate-900 font-medium">{{ booking.pickup }}</p>
              <p class="text-xs text-slate-600 mt-1 leading-relaxed">{{ booking.address }}</p>
            </div>
          </div>

          <div class="flex gap-3">
            <div class="flex-shrink-0 w-8 h-8 bg-red-100 rounded-full flex items-center justify-center">
              <svg class="w-4 h-4 text-red-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"></path>
              </svg>
            </div>
            <div class="flex-1">
              <p class="text-xs text-slate-500 font-semibold mb-1">Drop Location</p>
              <p class="text-sm text-slate-900 font-medium">{{ booking.drop }}</p>
            </div>
          </div>
        </div>








        <div class="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 flex items-center justify-center p-4">
  <div class="bg-white w-full max-w-sm rounded-2xl shadow-lg p-6 space-y-4">

    <h2 class="text-2xl font-bold text-slate-900 text-center">
      Set Your Availability
    </h2>
    <p class="text-xs text-slate-600 text-center">
      City & routes where you are available
    </p>

    @if (error) {
      <div class="bg-red-100 text-red-700 p-2 rounded-lg text-xs">
        {{ error }}
      </div>
    }

    <!-- CURRENT CITY -->
    <form [formGroup]="cityForm" class="space-y-2">
      <label class="text-xs font-bold text-slate-600">Current City</label>

      <input
        type="text"
        formControlName="city"
        placeholder="Enter current city"
        class="w-full px-4 py-2 border border-slate-300 rounded-lg text-sm" />

      <button
        type="button"
        (click)="updateCity()"
        [disabled]="loading"
        class="w-full bg-black text-white py-2 rounded-lg text-sm font-bold">
        {{ loading ? 'Updating...' : 'Update City' }}
      </button>
    </form>

    <hr />

    <!-- AVAILABLE ROUTES -->
    <form [formGroup]="routeForm" class="space-y-2">
      <p class="text-xs font-bold text-slate-600">
        Available Routes (Optional)
      </p>

      <div class="flex gap-2">
        <input
          type="text"
          formControlName="from"
          placeholder="From"
          class="w-1/2 px-3 py-2 border rounded text-sm" />

        <input
          type="text"
          formControlName="to"
          placeholder="To"
          class="w-1/2 px-3 py-2 border rounded text-sm" />
      </div>

      <button
        type="button"
        (click)="addRoute()"
        [disabled]="routeForm.invalid"
        class="w-full bg-slate-900 text-white py-2 rounded text-sm font-bold">
        Add Route
      </button>
    </form>

    <!-- ROUTE LIST -->
    @if (routes.length > 0) {
      @for (r of routes; track $index) {
        <div class="flex items-center justify-between bg-slate-100 px-3 py-2 rounded text-sm">
          <span class="truncate">
            {{ r.from }} → {{ r.to }}
          </span>
          <button
            (click)="removeRoute($index)"
            class="text-red-600 text-xs font-bold ml-2">
            Remove
          </button>
        </div>
      }
    }

    <button
      (click)="backToDashboard()"
      class="w-full text-blue-600 text-xs font-semibold">
      Back to Dashboard
    </button>

  </div>
</div>
